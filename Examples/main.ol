class Main is
  this is
    IO.Write("this".Concatenate(" is ").Concatenate(Strings().somestring))
  end
end

class Strings is
  this is end
  var unusedstring : "cool language"
  var somestring   : "weird language"
end
